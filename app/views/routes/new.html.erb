<div class="home_div">
<h1>New route</h1>
    <form class="form-inline" role="form" id="route_form">
      <div class="form-group">
        <select class="form-control" id="turn-select">
          <option value="morning">Morning</option>
          <option value="evening">Evening</option>
          <option value="night">Night</option>
        </select>
      </div>
      <div class="form-group">
        <select class="form-control" id="dir  ection-select">
          <option value="going">Going</option>
          <option value="returning">Returning</option>
        </select>
      </div>
      <button type="submit" id="route-generate" class="btn btn-success btn-large">Generate New Route</button>
    </form>
</div>

<!--<script>
  $(function() {
    return $("#route-generate").on("click", function() {
      var direction, turn;
      turn = $("#turn-select").val();
      direction = $("#direction-select").val();
      return window.location.href = "/generate_new_route/" + turn + "/" + direction;
    });
  });
</script>-->
